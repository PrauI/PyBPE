from setuptools import setup, find_packages

setup(
        name="pybpe",
        versioin="0.1.0",
        packages=find_packages(),
        include_package_data=True,
        description="A fast Byte Pair Encoding Generator",
)
